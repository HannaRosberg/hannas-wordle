{
  "name": "wordle-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm run build-client && npm run server",
    "server": "nodemon server.js",
    "build-client": "npm install --prefix ../wordle-game && npm run build --prefix ../wordle-game",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "all": "^0.0.0",
    "concurrently": "^7.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-handlebars": "^7.1.3",
    "mongoose": "^5.11.15",
    "nodemon": "^2.0.7"
  }
}
